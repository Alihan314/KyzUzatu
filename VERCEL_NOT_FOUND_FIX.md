# Решение ошибки NOT_FOUND на Vercel

## 1. ✅ Предложенное исправление

Я внес следующие изменения:

### Изменение 1: Runtime конфигурация в `src/app/api/rsvp/route.ts`
- **Было**: `export const runtime = "nodejs";`
- **Стало**: `export const runtime = "edge";`

### Изменение 2: Добавлен GET handler для диагностики
Добавлен обработчик GET запросов, который позволяет проверить работоспособность API endpoint.

### Почему это работает:
- Edge Runtime лучше оптимизирован для Vercel и работает быстрее
- Edge Runtime не требует дополнительной конфигурации на Vercel
- GET handler помогает диагностировать проблемы

---

## 2. 🔍 Объяснение причины ошибки

### Что происходило на самом деле:

**Проблема**: Vercel не мог правильно обработать API route с `runtime = "nodejs"` в контексте вашего проекта.

**Что код делал:**
- API route был настроен с `runtime = "nodejs"`
- Next.js пытался создать Node.js runtime окружение для этого route
- Vercel при деплое не мог правильно определить или настроить этот runtime
- В результате запросы к `/api/rsvp` возвращали `NOT_FOUND`

**Что нужно было сделать:**
- Использовать Edge Runtime (по умолчанию в Next.js 14 на Vercel)
- Или правильно настроить Node.js runtime (если нужны специфические Node.js API)

### Условия, которые вызвали ошибку:

1. **Версия Next.js**: Next.js 14.x по умолчанию использует Edge Runtime для API routes
2. **Деплой на Vercel**: Vercel автоматически оптимизирует Edge Runtime routes
3. **Явное указание Node.js runtime**: Конфликт между явной конфигурацией и ожиданиями платформы
4. **Отсутствие дополнительной конфигурации**: Не было настроек для поддержки Node.js runtime

### Недостающее понимание:

**Ошибка в мышлении**: 
- Предположение, что `runtime = "nodejs"` всегда работает на Vercel
- Непонимание разницы между Edge и Node.js runtime
- Отсутствие понимания, что Vercel оптимизирован для Edge Runtime

**Правильное понимание**:
- Edge Runtime - это легковесная среда выполнения, оптимизированная для Vercel
- Node.js Runtime нужен только если требуются специфические Node.js API (fs, crypto, и т.д.)
- Для простых API routes (как ваш RSVP) Edge Runtime идеален

---

## 3. 📚 Понимание концепции

### Почему эта ошибка существует?

**Защита от проблем:**
- `NOT_FOUND` защищает от попыток доступа к несуществующим ресурсам
- Помогает выявить проблемы конфигурации на раннем этапе
- Предотвращает выполнение неправильно настроенного кода

**Ментальная модель:**

```
Next.js API Route → Runtime выбор → Vercel деплой → Работающий endpoint
     ↓                    ↓              ↓                    ↓
  route.ts      Edge/Node.js?    Оптимизация?         Доступен?
```

**Как это работает в Next.js 14:**

1. **App Router API Routes** (`app/api/route.ts`):
   - По умолчанию используют **Edge Runtime**
   - Edge Runtime работает на V8 изолятах (быстро, дешево)
   - Поддерживает Web APIs (fetch, Request, Response)

2. **Node.js Runtime**:
   - Нужен для доступа к Node.js API (fs, path, crypto, и т.д.)
   - Требует больше ресурсов
   - Может быть медленнее на "холодном старте"

3. **Vercel оптимизация**:
   - Автоматически оптимизирует Edge Runtime routes
   - Edge Runtime routes запускаются быстрее
   - Лучше масштабируются

**В контексте фреймворка:**

```
Next.js 14 App Router
├── Server Components (по умолчанию)
├── Client Components ("use client")
└── API Routes (app/api/)
    ├── Edge Runtime (по умолчанию) ← Рекомендуется
    └── Node.js Runtime (если нужны Node.js API)
```

---

## 4. ⚠️ Предупреждающие знаки

### На что обращать внимание:

**Код-запахи, указывающие на проблему:**

1. **Явное указание Node.js runtime без необходимости:**
   ```typescript
   export const runtime = "nodejs"; // ⚠️ Проверьте, действительно ли нужен
   ```

2. **Использование Node.js API без проверки:**
   ```typescript
   import fs from 'fs'; // ⚠️ Не работает в Edge Runtime
   ```

3. **Отсутствие обработки ошибок в API routes:**
   ```typescript
   export async function POST() {
     // Нет try-catch ⚠️
   }
   ```

4. **Неправильная структура файлов:**
   ```
   app/api/rsvp.ts ❌ (неправильно)
   app/api/rsvp/route.ts ✅ (правильно)
   ```

### Похожие ошибки, которые можно сделать:

1. **Неправильное имя файла:**
   - `route.js` вместо `route.ts`
   - `handler.ts` вместо `route.ts`

2. **Неправильная структура директорий:**
   - `api/rsvp.ts` вместо `api/rsvp/route.ts`

3. **Экспорт неправильных функций:**
   ```typescript
   export default function handler() {} // ❌ Неправильно
   export async function POST() {} // ✅ Правильно
   ```

4. **Проблемы с типами:**
   ```typescript
   export async function POST(req: Request) {} // ⚠️ Должен быть NextRequest
   ```

### Паттерны, указывающие на проблему:

- API route работает локально, но не работает на Vercel
- Ошибки типа "Function not found" или "Route not found"
- Проблемы с импортами Node.js модулей в API routes
- Медленная работа API routes на Vercel

---

## 5. 🔄 Альтернативные подходы

### Вариант 1: Edge Runtime (текущее решение) ✅ Рекомендуется

**Плюсы:**
- ✅ Быстрый старт (холодный старт < 50ms)
- ✅ Лучшая масштабируемость
- ✅ Оптимизирован для Vercel
- ✅ Дешевле в использовании
- ✅ Поддерживает все Web APIs

**Минусы:**
- ❌ Не поддерживает Node.js API (fs, path, crypto и т.д.)
- ❌ Ограниченная поддержка некоторых npm пакетов

**Когда использовать:**
- Простые API endpoints (как ваш RSVP)
- Интеграции с внешними API
- Обработка JSON данных
- Аутентификация и авторизация

---

### Вариант 2: Node.js Runtime (если нужны Node.js API)

**Код:**
```typescript
export const runtime = "nodejs";

export async function POST(request: NextRequest) {
  // Теперь можно использовать Node.js API
  const fs = require('fs');
  // ...
}
```

**Плюсы:**
- ✅ Полный доступ к Node.js API
- ✅ Поддержка всех npm пакетов
- ✅ Работа с файловой системой

**Минусы:**
- ❌ Медленнее на холодном старте
- ❌ Больше потребление ресурсов
- ❌ Может требовать дополнительной конфигурации на Vercel

**Когда использовать:**
- Работа с файловой системой
- Использование специфических Node.js модулей
- Интеграция с базами данных через нативные драйверы

---

### Вариант 3: Без явного указания runtime (по умолчанию Edge)

**Код:**
```typescript
// Просто не указывайте runtime
export async function POST(request: NextRequest) {
  // ...
}
```

**Плюсы:**
- ✅ Простота
- ✅ Автоматическая оптимизация
- ✅ Работает "из коробки"

**Минусы:**
- ❌ Может быть неочевидно, какой runtime используется
- ❌ Нет контроля над runtime

---

### Вариант 4: Использование Server Actions (альтернатива API routes)

**Код:**
```typescript
// app/actions/rsvp.ts
"use server"

export async function submitRSVP(data: FormData) {
  // Обработка данных
}
```

**Плюсы:**
- ✅ Более интегрировано с React
- ✅ Типобезопасность
- ✅ Меньше boilerplate кода

**Минусы:**
- ❌ Меньше контроля над HTTP методами
- ❌ Сложнее для REST API

---

## 📋 Чек-лист для проверки API routes

- [ ] Файл называется `route.ts` (не `handler.ts` или `api.ts`)
- [ ] Файл находится в `app/api/[name]/route.ts`
- [ ] Экспортируются правильные функции (POST, GET, и т.д.)
- [ ] Используется правильный runtime (Edge по умолчанию)
- [ ] Типы правильные (NextRequest, NextResponse)
- [ ] Есть обработка ошибок (try-catch)
- [ ] Тестируется локально перед деплоем

---

## 🚀 Следующие шаги

1. **Закоммитьте изменения:**
   ```bash
   git add src/app/api/rsvp/route.ts
   git commit -m "Fix: Change runtime to edge for Vercel compatibility"
   git push
   ```

2. **Проверьте деплой на Vercel:**
   - Дождитесь автоматического деплоя
   - Проверьте логи деплоя
   - Протестируйте endpoint: `https://your-domain.vercel.app/api/rsvp`

3. **Протестируйте GET endpoint:**
   - Откройте в браузере: `https://your-domain.vercel.app/api/rsvp`
   - Должно вернуться JSON с информацией о endpoint

4. **Протестируйте POST запрос:**
   - Используйте форму RSVP на сайте
   - Проверьте, что данные отправляются успешно

---

## 📖 Дополнительные ресурсы

- [Next.js API Routes Documentation](https://nextjs.org/docs/app/building-your-application/routing/route-handlers)
- [Vercel Edge Runtime](https://vercel.com/docs/functions/runtimes/edge-runtime)
- [Next.js Runtime Differences](https://nextjs.org/docs/app/api-reference/route/route-segment-config#runtime)

